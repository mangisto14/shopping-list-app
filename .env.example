VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional. Canonical base URL used to build invite links (e.g.
# https://mangisto.best). If unset, falls back to window.location.origin
# at runtime - safe to leave empty for local dev and preview deploys.
VITE_APP_URL=
